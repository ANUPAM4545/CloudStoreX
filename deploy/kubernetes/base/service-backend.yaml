apiVersion: v1
kind: Service
metadata:
  name: cloudstorex-backend
  labels:
    app.kubernetes.io/name: cloudstorex
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cloudstorex
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
