# API Keys

API Keys are tightly scoped programmatic tokens for Developers.
A unique prefix allows rapid resolution. Scopes can limit keys to read-only capabilities without utilizing a full Service Account workflow.
