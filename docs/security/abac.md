# Attribute-Based Access Control (ABAC)

ABAC allows extremely granular authorization constraints.
Conditions map against `ContextAttributes` (e.g. Device ID, IP Address) and `ResourceAttributes` (e.g. Regions, Providers, Tags) for fine-grained rule adherence.
